# Copy to `frontend/.env` for local dev, or set in CI when running `npm run build`.
# Create React App reads these at build time only.
#
# Backend origin (no trailing slash). The app calls `{this}/api/...`.
REACT_APP_BACKEND_URL=http://localhost:8000
